[project]
name = "medical-rag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "qdrant-client",
    "requests",
    "python-dotenv",
    "pypdf",
    "torch",
    "transformers",
    "huggingface_hub",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.1",
    "pydantic>=2.13.4",
]

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu126" }
